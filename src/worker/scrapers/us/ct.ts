import { PlateResult } from "../base";

// Connecticut (CT) — personalized plate availability scraper
// Status: NOT YET IMPLEMENTED
// TODO: Find the availability check endpoint at the Connecticut DMV/SOS website,
// reverse-engineer the request in DevTools, then implement similar to FL or TX scraper.

export async function scrapeCT(): Promise<PlateResult[]> {
  console.log("[CT] Scraper not yet implemented");
  return [];
}
