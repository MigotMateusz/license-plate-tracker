import { PlateResult } from "../base";

// Hawaii (HI) — personalized plate availability scraper
// Status: NOT YET IMPLEMENTED
// TODO: Find the availability check endpoint at the Hawaii DMV/SOS website,
// reverse-engineer the request in DevTools, then implement similar to FL or TX scraper.

export async function scrapeHI(): Promise<PlateResult[]> {
  console.log("[HI] Scraper not yet implemented");
  return [];
}
